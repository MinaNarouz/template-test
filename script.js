// In JS you can use WINDOW which is very useful in interacting with browser interactions with user.
// Also you can use window.console.log to display things in browser which is useful for debugging and seeing what an API data is.

console.log("testing");